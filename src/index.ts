import { WorldModel } from "./WorldModel.js";

const numRows = 10;
const numCols = 15;
const worldWidth = 800;
const worldHeight = 600;

const world = new WorldModel(numRows, numCols, worldWidth, worldHeight);
